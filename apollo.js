const apolloClientOptions = {
  uri: "https://slog-deeptime-backend.herokuapp.com",
  // uri: `http://${Platform.OS === "ios" ? "localhost" : "192.168.0.229"}:4000`,
}

export default apolloClientOptions
